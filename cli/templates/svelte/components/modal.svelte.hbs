<script lang="ts">
/**
 * @fileoverview {{componentName}} Component - Enterprise Standards Compliant Svelte
 * @description modal component using SvelteKit patterns
 * @version 5.0.0
 * @compliance WCAG 2.2 AAA, Token-first, SSR-safe, No stores
 */
import { cva, type VariantProps } from 'class-variance-authority';
import { t } from '@xala-technologies/ui-system/i18n';

// CVA Variants
const modalVariants = cva(
  'modal-base',
  {
    variants: {
      variant: {
        default: 'variant-default',
        primary: 'variant-primary',
        secondary: 'variant-secondary',
      },
      size: {
        sm: 'size-sm',
        md: 'size-md',
        lg: 'size-lg',
      },
    },
    defaultVariants: {
      variant: 'default',
      size: 'md',
    },
  }
);

// Props interface
export let variant: 'default' | 'primary' | 'secondary' = 'default';
export let size: 'sm' | 'md' | 'lg' = 'md';
export let className: string = '';

// v5.0 Architecture: Static values, CSS handles theming
$: classes = modalVariants({ variant, size }) + ' ' + className;
</script>

<div 
  class={classes}
  data-component="modal"
  data-variant={variant}
  data-size={size}
>
  <slot />
</div>

<style>
/* v5.0 Architecture: CSS custom properties for theming */
.modal-base {
  --component-bg: var(--background);
  --component-border: var(--border);
  --component-text: var(--foreground);
}
</style>